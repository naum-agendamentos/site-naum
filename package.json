{
  "name": "na.um",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "postinstall": "cd site-naum && npm install && npm run build",
    "serve": "serve -s build"
  },
  "dependencies": {
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "react-scripts": "4.0.3",
    "serve": "^11.3.2"
  }
}
